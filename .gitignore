# OS
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Claude Code local (personal) settings
.claude/settings.local.json

# Logs / temp
*.log
tmp/
scratchpad/

# Dependencies (in case tooling is added later)
node_modules/

# Next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# Env files (secrets) — keep .env.example committed, never the real ones
.env
.env*.local

# Load-test auth cookie (contains a live session token — never commit)
scratch-auth-cookie.txt
